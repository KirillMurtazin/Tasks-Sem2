#include "Point.h"

// Реализация конструктора
Point::Point(double xCoord, double yCoord)
    : X(xCoord),   // Инициализация X через список инициализации
    Y(yCoord)    // Инициализация Y
{
    // Тело конструктора пустое, так как инициализация выполнена выше
}